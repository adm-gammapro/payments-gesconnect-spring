package com.raissapayments.conector.domain.repository.operativo;

import com.raissapayments.conector.domain.entity.operativo.ResponseTransferenciaEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ResponseTransferenciaRepository extends JpaRepository<ResponseTransferenciaEntity, Long> {
}
